# -*- mode: snippet -*-
# name: header
# key: header
# --

;;; `(f-filename (buffer-file-name))` --- $1 -*- lexical-binding: t -*-
;;
;;; Commentary:
;;
;;; Code:

$0

;;; `(f-filename (buffer-file-name))` ends here